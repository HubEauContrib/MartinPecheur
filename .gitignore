# Flutter / Dart â€” la racine est le projet Flutter.
# `android/`, `ios/` et `windows/` sont VERSIONNÃ‰S : ils se modifient Ã  la main
# et portent la signature de release (ADR-013).
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
*.iml
.idea/
.vs/
windows/flutter/ephemeral/
android/.gradle/
android/local.properties

# Signature de release Android â€” clÃ© et propriÃ©tÃ©s jamais versionnÃ©es
android/key.properties
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision

# Secrets et environnement local
.env*.local
*.pem

# SystÃ¨me et Ã©diteurs
.DS_Store
*.swp

# Claude Code â€” rÃ©glages personnels, propres Ã  la machine
# (.claude/settings.json et .claude/commands/ restent versionnÃ©s : config d'Ã©quipe)
.claude/settings.local.json
